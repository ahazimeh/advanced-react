what to test:
    Look at each individual part of your application
    Imagine telling a friend 'heres what this price of code does'
    write a test to verify each part does what you expect
npm i redux react-redux
npm i enzyme enzyme-adapter-react-18

wrapped.find('textarea').prop('value')

npm i axios redux-promise moxios

an error happened when we tried to use axios to get data from outside world, so we use moxios to fake requests